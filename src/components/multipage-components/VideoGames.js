import { Layout } from '../shared-components/Layout.js';

export function VideoGames() {
    return (
        <Layout>
            <h2>
                Video Game Page
            </h2>
        </Layout>
    );
}